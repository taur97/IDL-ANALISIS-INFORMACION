# AMASBA IDL Dashboard (Streamlit)

Dashboard de ingresos y salidas de mineral conectado a MySQL (cPanel).

## Setup local
```bash
python -m venv IDL
.\IDL\Scripts\Activate.ps1
pip install -r requirements.txt
